import Box from '@mui/material/Box';

const SuccessScreen = (): JSX.Element => {
    return (
        <Box>The email has been sent to your email address successfully.</Box>
    );
};

export default SuccessScreen;
